[package]
name = "project_euler"
version = "0.0.1"
authors = ["Josh Gadeken"]
edition = "2018"

[dependencies]

[dev-dependencies]
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"

[lib]
name = "libeuler"
path = "src/libeuler.rs"
